#include "holberton.h"

/**
  * free2 - free double of array
  *
  * @tokens: matrix
  * Return: void
  */
void free2(char **tokens)
{
	int i = 0;

	if (!tokens)
		return;

	while (tokens[i])
		free(tokens[i++]);

	free(tokens);
}
